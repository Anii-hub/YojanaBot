# ── Steps 1–3 (data pipeline + vector store) ──────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
pymupdf==1.24.10
pydantic==2.9.2
tqdm==4.66.5
chromadb==0.4.24
# v2.7.0 uses transformers~=4.36 which does NOT import torch.distributed.tensor
# at module level. v3.x pulls in transformers>=4.47 which OOMs on 512 MB free tier.
sentence-transformers==2.7.0
numpy==1.26.4

# ── Steps 4–7 (RAG chain, formatting, language support) ───────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
groq>=0.11.0
deep-translator>=1.11.0
python-dotenv>=1.0.0

# ── Web UI (Django) ────────────────────────────────────────────────────────
django>=4.2.0
whitenoise>=6.7.0
gunicorn>=21.2.0
